# Act 1 — the naive loop (gates off)

axe violations on login.html: **4 → 2** (down 2). The counter says success.

What actually happened to real users:
- 'Remember Me' is gone for everyone (display:none).
- The Google and Facebook sign-in buttons are deleted — real functionality removed.
- The Forgot-Password link is off-screen — still in the tab order, invisible on screen.

Every move here is a RUBRIC section-4 banned pattern. Mend's gate 3 flags: **interactive-deleted, display-none-added** — so in Mend, all of these rounds REVERT. "This is what 'just give it to Claude' produces. The counter lies."
